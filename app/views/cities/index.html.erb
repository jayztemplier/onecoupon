<% title "Cities" %>

<table>
  <tr>
    <th>Name</th>
  </tr>
  <% for city in @cities %>
    <tr>
      <td><%= city.name %></td>
      <td><%= link_to "Edit", edit_city_path(city) %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New City", new_city_path %></p>
